# MCP

This directory will hold shared MCP tool manifest definitions.

First-wave scope:

- one ATHENA read-oriented tool manifest

Write-path manifests and broader gateway-facing tool expansion are deferred until the first read slices are real.
